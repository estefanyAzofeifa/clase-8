/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package prueba.clase8;

import javax.swing.JOptionPane;

/**
 *
 * @author Aulas Heredia
 */
public class PruebaClase8 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        

      // primer ejercio esta subido en teams
         /*
      Del ejercicio 1, solicite un número que el usuario desee buscar en el arreglo.
Realice un método que le indique al usuario en cuál posición del arreglo se encuentra el número.
Valide si el usuario quiere buscar un número que no se encuentra en el arreglo.      */  
    /*int cantidadRegistro = Integer.parseInt(JOptionPane.showInputDialog("dijite la de registro"));
    int edad[] = new int [cantidadRegistro];
    solicitoDatosVector(edad);
    imprimDatosVector(edad);
}
    public static void solicitoDatosVector (int vector[])}
    for*/
       
     
    
             
       // solicito la informacion
        /* int edad[] = new int[3];
        for (int i = 0; i < edad.length; i++) {
            edad[i] = Integer.parseInt(JOptionPane.showInputDialog("dijite la edad"));

        }
        //imprime la 
        for (int i = 0; i < edad.length; i++) {
            JOptionPane.showMessageDialog(null, "la edad " + (i + 1) +": "+ edad[i]);

        }
    }*/

        
        int cantidadRegistros = Integer.parseInt(JOptionPane.showInputDialog("dijite la cantida de registros"));
        int edad [][]= new int [cantidadRegistros][cantidadRegistros];
        solicitaDatos bn
        
    }
}
